import {AvatarLetterPipe} from './avatar-letter.pipe';

describe('AvatarLetterPipe', () => {
    it('create an instance', () => {
        const pipe = new AvatarLetterPipe();
        expect(pipe).toBeTruthy();
    });
});
